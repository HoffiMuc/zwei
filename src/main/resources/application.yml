management:
  info:
    git:
      mode: "full"
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "*"

spring:
  cloud:
    stream:
      bindings:
        input:
          destination: my.eins.OutputChannel
      default-binder: local_rabbit
      binders:
        local_rabbit:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: localhost
                port: 5672

---

spring:
  profiles: vm
  cloud:
    stream:
      default-binder: local_rabbit
      binders:
        local_rabbit:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: 192.168.99.100
                port: 5672

